# CLIP Paper Reading Notes

## 今日内容
- i read the introduction and abstract of the original paper of CLIP.  It uses 零样本迁移 to continue to train the data has been frozen.
  accuracy in ImageNet got the same efficiency of ResNet50. However, ResNet50 use extra 128million lable picture to train it. This paper uses the angle of 从文字监督学习视觉 to solve the
  problem of pre-training.

  prompt engineering: it is an essential and interesting part of the method. If you use "a photo of cat" instead of "cat", it can get better efficient. Because the kind of sentence is
  similar with the data has been trained.
   
- 理解 image encoder 和 text encoder : use i-encoder to convert image into vetcor. use t-encoder convert text into vetcor. And compare the difference between by cos.
- 理解 contrastive learning 的基本思想

## 关键词
CLIP, contrastive learning, multimodal alignment
