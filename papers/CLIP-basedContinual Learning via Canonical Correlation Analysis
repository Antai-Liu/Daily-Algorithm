基于CLIP的持续学习
motivation：CLIP中大多只关注单个模态，但是没有发现视觉和文本之间的同步程度。
            通过实验证明了，image的变化速度比text快，导致Asymmetric Drift（非对称漂移）。
背景：持续学习，对于已经训练好的模型，如果继续来新的数据，如果重新训练，消耗太大，性价比低。->所以先不动前面的（冻结）。
冻结：不让模型再训练参数，参数不再更新
一般是：算 loss → 反向传播 → 更新 CLIP 参数
CLIP:输入图片 → CLIP 输出特征 → 后面继续算东西 → 但 CLIP 参数不更新
和微调的方法lora有点类似
还有好几种 1：PEFT 参数高效微调  lora，adapter等等
          2 prompt learning 提示学习    prompt tuning， prefix tuning等
          3 continual learning 持续学习  .......
 一：像今天我做的SFT是全参数调，调batch ，lr，显卡使用个数，max-step等等 LoRA 全称叫做 Low-Rank Adaptation低秩适配。就比如
一个大矩阵，sft是全部调整这个矩阵所有数字，但是LoRA是在这个矩阵的基础上加一个小的该变量，Δ矩阵。 所以变化的少，不动原先的，所以叫冻结。
二：prompt learning 模型学习 prompt， 学习一个最好的提示词。用提示词引导模型来解决问题
三：持续学习，新任务不断地来，我们不能忘记旧的任务。风险是灾难性遗忘，新旧任务的冲突。
ps：灾难性遗忘（Catastrophic Forgetting）：在机器学习中，参数w（weight）继承了学习的“记忆和经验”，每一次学习的内弄是通过w来呈现的。
  当我们进行新的任务训练是，w会往新的任务上靠拢，学习新的任务的经验，这时w变了，随着不断地变化，w会逐渐远离刚开始学习时积累的“经验”。所以
  导致了新旧任务的冲突，类似yu泛化。域偏移


这篇论文的第一步是进行一个小的训练（之后不会再训练了），在第一个任务上进行“热身”。让clip轻微调整，然后冻结。目的：拿到一定的灵活性

第二步：新的数据不断进来，我们不进行训练，把他们累加起来，但累加的对象不是原始数据，而是“摘要”：这个摘要具体指的是什么？
通过这个摘要来记录他的“经验”这个相当于另一种形式的w。
论文用的是"运行均值 + 协方差递归更新"进行的这个“摘要”的操作。

第三步：任务结束之后，重新校准一次坐标系，用摘要统计去解决CCA问题  （有点抽象）             闭式解（closed-form）
        有四个阶段
        第一：白化：把一批数据“洗干净”，让它们变得标准、均匀、彼此不粘连。让特征的尺度变一样，让特征之间尽量不相关。
              让方差变小，特征的相关性降低。
                          在机器学习中的作用   减少特征冗余
                                              有些特征信息重复，白化可以降低重复性。
                                              让模型更容易训练
                                              数据分布更规整，优化更稳定。
                                              加快收敛
                                              梯  度下降时，不容易在狭长的椭圆谷里来回震荡。
                                              常用于 PCA、图像处理、特征预处理
                                              比如 PCA Whitening、ZCA Whitening。
              还有一个是标准化。区别：.......
      第二：白化后的数据，看他们的真实联动，他们之间的规律

      第三：找出"联动最强的那几个方向"

      第四：保留最重要的几个方向，扔掉噪声。
这就是所有的cca的过程。






