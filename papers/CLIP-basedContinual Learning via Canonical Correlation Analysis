Based on CLIP, this paper solves a problem that the image changes fast than text by using an old method.
持续学习与灾难性遗忘，冻结预训练模型做特征提取。 但是还有一个问题是视觉偏移。eg：猫和狗的图片，image差距很淡，但是加上prompt的text有时只改变了一点
思路:要拉近距离但不能靠激进微调 CLIP(会破坏预训练知识),所以换空间——用 CCA。最后列三条贡献:提 CCA 框架解非对称漂移、用 RFP 做非线性扩展、四数据集实验证明性能与高效。
CLIP-CL 方法有两类：冻结  1  CLIP 加模块
                        2  微调 CLIP 或 PEFT
都有问题：非对称漂移（Asymmetric Drift）非对称漂移

方法：定义视觉漂移 Δv、文本漂移 Δt、模态距离 dist(t)
    根据实验数据：Δv 始终大于 Δt、距离随任务上升、准确率下降——>由此引出"投到共享子空间而不动 CLIP"的动机。
CCA-CL （典型相关分析，hotelling 1936）：他能找到两个投影矩阵a，b，最大化ATX 和ATB的相关性，而且具闭式解

1。给出协方差的递归累加(公式 9)
    随着mini-batch 不断更新 Σxx、Σyy、Σxy；
      essential：只保存协方差矩阵，就隐式保留了历史数据分布，不需要存样本（exemplar-free），这是它的关键卖点。
    白化：对 Σxx、Σyy 特征分解，得到白化变换 Wx、Wy。
    交叉协方差 ：C = Wx Σxy Wy
    对 K = CC⊤ 特征分解，得到典型基 V 和典型相关 Λρ
    能量准则选 top-r（公式14），阈值 η 通常 0.99——保留主要相关能量，丢掉噪声维度。
构造投影矩阵 A = Wx Vr，B = Wy(C⊤Vr)Λr_ρ⁻¹（公式15），把两模态投到共享子空间（公式16）。




RFP    Random Fourier Projection非线性扩展:在累加协方差前先对图文特征做随机余弦映射,近似 RBF 核、零可训练参数。
z′ = √(2/D′)·cos(Wz + b)，W 采自 N(0, σ⁻²I)，b 采自 [0,2π]。
把特征升到高维，让非线性相关变得线性可分，且不引入任何可训练参数（仍然 training-free）。
代价是内存——表3显示宽度从 2048 升到 10240，显存从 3.1GB 涨到 11.9GB。



 四、实验关键结论（你做对比时要心里有数）

设定：CIFAR-100 / ImageNet-R / ImageNet-100 / CUB-200，各切 10 个任务，CLIP ViT-B/16，单卡 4090。
效果：Last Accuracy 在 4 个数据集全最优，CIFAR-100 超第二名 3.8%。但 ImageNet-100 的 Avg 上输给了 CLAP/RAPF（这俩用了 replay）——说明它不是无敌。
效率：CIFAR-100 上 5.8 分钟（去掉 FTT 只要 3.9 分钟），RAPF 13.9 分钟，ENGINE/SLCA 80+ 分钟。这是它最硬的优势。
消融（表2）：全冻结 baseline 66.7% → +CCA 76.5%（+9.8，最大增益来自 CCA）→ +RFP 81.0% → +FTT 83.0%。
First-Task Training (FTT)：只在第一个任务上微调（SGD，lr=1e-6，5 epoch），之后全冻结。



重述发现的非对称漂移问题、用 CCA 求共享子空间最小化图文距离的解法、以及 RFP 补非线性;强调精度与效率双优
。坦诚局限——仍依赖第一个任务的微调;未来计划引入参数高效微调以提升可扩展性与稳定性
